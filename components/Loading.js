import {Circle} from 'better-react-spinkit'

function Loading() {
    return (
        <div>
            Loading...

            <Circle color='#3cbC28' size={60} />
        </div>
    )
}

export default Loading
